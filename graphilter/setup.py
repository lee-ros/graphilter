from distutils.core import setup

setup(
    name='graphilter',
    version=0.1,
    description='Graph based signal processing tool',
    author='Lee Rosenberg',
    packages=[
        'graphilter',
    ]
)